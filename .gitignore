# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js

# Exclude sourcemaps
*.map

# obsidian
# 本地数据文件：插件目录内的历史数据与 2.5.4 新增的镜像兑底文件
# （均属用户运行时数据，不入库；.obsidian 配置目录下的主数据文件不在仓库内）
data.json
position-cache.json
md-razor-settings.mirror.json
md-razor-position-cache.mirror.json

# Claude Code metadata
.agents/
.claude/skills/
skills-lock.json

# WorkBuddy AI metadata
.workbuddy-ai/

# Exclude macOS Finder (System Explorer) View States
.DS_Store
