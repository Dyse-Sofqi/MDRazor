# 2.4.8

## 修复
- 修复延迟输入框内联样式导致的 lint 告警（`obsidianmd/no-static-styles-assignment`）：改用 CSS 类 `mdrazor-lazy-delay-input` 统一控制 60px 固定宽度，不再直接设置 `element.style`。

## 更新
- `minAppVersion` **1.1.0**（使用 `setTooltip` API）。
- 懒加载列表 UI 优化：
  - 插件设置项改为 **两栏网格** 展示，窄屏自动回退单栏；
  - 移除插件简介，列表更紧凑；
  - 插件名垂直居中并美化（加粗、长名省略）；
  - 调整内边距，插件名与启用开关不再贴近边界。

## 新增
- **懒加载标签页**：在设置界面新增「懒加载」页，可为各社区插件单独设置启用开关与启动延迟（秒）。被标记懒加载的插件启动时不随 Obsidian 立即加载，而是等待设定延迟后再加载，各插件延迟的相对大小即构成启动顺序。
- **「立即检查」按钮**：位于「启用懒加载」开关前（lucide-timer 图标），点击弹出启动耗时检查弹窗——按「延迟 x s，启动耗时 x ms」展示所有「已启用且延迟启动」插件的实测加载耗时（含 onload）与配置延迟，并附环境信息与「复制」全文。
- **界面多语言（i18n）**：依据 Obsidian 界面语言自动显示中文或英文界面（zh → 中文，其余 → 英文）。
- 说明：原生「立即检查」弹窗与其计时数据不对外暴露，本插件复用同一按钮交互并自实现等效统计。

## 文件
- `main.js` / `manifest.json` / `styles.css`
